node_modules/
.next/
.open-next/
dist/
coverage/
.env*
!.env.example

# npm lockfile is intentionally not committed (repo manages deps without one;
# the Cloudflare build generates it fresh at install time).
package-lock.json

# Local screenshot output (scripts/screenshot.mjs writes to /tmp by default).
/shots/
